{
  "rules": {
    "presence": {
      "$boardId": {
        ".read": "auth != null",
        "$sessionId": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  }
}
